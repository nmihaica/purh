 // write full response locally
    /* dayjs.extend(customParseFormat)
    // oib poslovnog prostora
    let OIB1 = invoice.oib
    // oib operatera
    let OIB2 = invoice.oibOper
    // ukupan iznos računa
    let UKUPNO = invoice.iznosUkupno
    let REDNI_BROJ = invoice.brRac.bor
    fs.writeFileSync(env.ROOT_PATH + `/logs/racuni/responses/${OIB1}_${OIB2}_${REDNI_BROJ}.xml`, res.data, 'utf8'); */